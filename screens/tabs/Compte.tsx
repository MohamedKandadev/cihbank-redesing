import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

type Props = {};

const Compte = (props: Props) => {
  return (
    <View>
      <Text>Compte</Text>
    </View>
  );
};

export default Compte;

const styles = StyleSheet.create({});
