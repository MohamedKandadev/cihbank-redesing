import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

type Props = {};

const Cartes = (props: Props) => {
  return (
    <View>
      <Text>Cartes</Text>
    </View>
  );
};

export default Cartes;

const styles = StyleSheet.create({});
